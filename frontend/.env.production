# Production environment — Next.js frontend
# Backend runs on same server at port 5100

# Used by Next.js server-side (SSR + rewrites) to reach the backend internally
BACKEND_API_URL=http://localhost:5100

# Public API base — keep empty so browser uses relative paths through Next.js rewrites
NEXT_PUBLIC_API_URL=
